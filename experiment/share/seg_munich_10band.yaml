class_path: meta_embedding.datamodules.seg_munich.module.SegMunichDataModule
init_args:
  root_path: "/mnt/disk/xials/dataset/TUM_128"
  padding: False
  dataloader_args:
    # all experiment use same batch size and same learning rate
    batch_size: 16
    num_workers: 8
    pin_memory: True
    shuffle: True